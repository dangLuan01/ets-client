services:
  web:
    build:
      dockerfile: Dockerfile.staging
    container_name: client_app_ets
    env_file: .env
    expose:
      - "3000"
    environment:
      - VIRTUAL_HOST=vidhub.io.vn
      - LETSENCRYPT_HOST=vidhub.io.vn
      - LETSENCRYPT_EMAIL=ml2673572@gmail.com
      - VIRTUAL_PORT=3000
    networks:
      - app-network
    restart: unless-stopped
networks:
  app-network:
    external: true
    name: app-network
